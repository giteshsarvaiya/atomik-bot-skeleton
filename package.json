{
  "name": "atomik-bot-skeleton",
  "version": "1.0.0",
  "description": "this is a skeleton for testing purpose",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write .",
    "migrate": "node -e \"import('./db/database.js').then(m => m.initializeDatabase()).then(() => process.exit(0)).catch(() => process.exit(1))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-personal:giteshsarvaiya/atomik-bot-skeleton.git"
  },
  "keywords": [
    "atmoik"
  ],
  "author": "giteshsarvaiya",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.2",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "eslint": "^9.34.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "nodemon": "^3.0.3",
    "prettier": "^3.6.2"
  }
}
